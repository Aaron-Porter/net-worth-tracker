# Convex
NEXT_PUBLIC_CONVEX_URL=

# Set these in Convex dashboard (npx convex env set):
# - JWT_PRIVATE_KEY (generate with: openssl genrsa 2048 | openssl pkcs8 -topk8 -nocrypt)
# - JWKS (public key in JWK format)
# - SITE_URL (e.g., http://localhost:3000)
