'use client'

// Re-export from new location for backward compatibility
export {
  TrackedValue,
  SimpleTrackedValue,
  InlineCalculation,
  CalculationCard,
  useTrackedValue,
} from '../../components/TrackedValue'
export type { SimpleInput } from '../../components/TrackedValue'
